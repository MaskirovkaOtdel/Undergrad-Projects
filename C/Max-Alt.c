    // Υπολογισμός του μέγιστου αριθμού
    if (num1 >= num2) { 
 		if(num1>=num3)
 			max=num1
 			printf("Megistos einai o %.3f \n", max);
 		else
 			max=num3
 			printf("Megistos einai o %.3f \n", max);
 	else
 		if(num2>=num3)
 			max=num2
 			printf("Megistos einai o %.3f \n", max);
 		else
 			max=num3
 			printf("Megistos einai o %.3f \n", max);


    return 0;
}
